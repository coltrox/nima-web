# Nima — painel web + landing

Projeto de TCC. React 19 + Vite + react-router 7 + styled-components.
Deploy: Hostinger (Apache, build estático em public_html).

- `/` = landing pública, `/login` = painel de ONG e admin
- Não mexer nos dashboards sem pedir
- API vem de import.meta.env.VITE_API_URL (nunca hardcode localhost)